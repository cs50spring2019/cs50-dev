# This file is sourced by csh or tcsh in every shell.  After editing
# this file, type `source .cshrc' in any shell which is already open
# and in which you want the changes to take effect.  For more info
# type `man csh' or `man tcsh'.

# The latest official version of this file is available in ~testuser
# with earlier versions available under RCS control.

# --------------------------------------------------------------------

# Which printer is your default?

setenv PRINTER s005

# --------------------------------------------------------------------

# Where are the manpages kept?

setenv MANPATH '.:/usr/man:/usr/X11R6/man:/usr/share/catman:/usr/share/catman/p_man:/usr/share/man:/usr/contrib/man:/usr/local/man'

# Where are the X application default resource files?

setenv XUSERFILESEARCHPATH ./%N.ad:$HOME/%N:$HOME/lib/app-defaults/%N:/usr/contrib/lib/app-defaults/%N

# --------------------------------------------------------------------

# Use less as a paginator instead of more (e.g. for man pages).

setenv PAGER less

# Make less more verbose than usual.

setenv LESS '-s -w -M'

# Make gzip more verbose than usual.

setenv GZIP '-v'

# --------------------------------------------------------------------

# Where are we?

setenv DOMAIN dartmouth.edu

# What is our homepage?  This is used by lynx, for example.

setenv WWW_HOME http://www.cs.dartmouth.edu/~$USER/

# ----------------------------------------------------------------------

# What editor should we use?

setenv EDITOR '/usr/bin/emacs -nw -q'

# If you use gnuserv in XEmacs, use this instead:
#
#	setenv EDITOR /usr/contrib/bin/gnuclient
# or
#	setenv EDITOR /usr/contrib/bin/gnuattach
#
# For more info, type `man gnudoit'.

# --------------------------------------------------------------------
